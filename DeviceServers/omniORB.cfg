# omniORB client configuration for faster Tango DB calls
# Timeouts in milliseconds
clientCallTimeoutPeriod = 5000

# Enable bootstrap agent
supportBootstrapAgent = 1

# Direct NameService reference to local Tango DB
ORBInitRef NameService = corbaloc:iiop:127.0.0.1:10000/NameService
